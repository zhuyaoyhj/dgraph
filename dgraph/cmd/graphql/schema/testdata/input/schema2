type Post {
  id: ID!
  title: String!
  text: String
  author: Author! @hasInverse(field: "posts")
  postType: PostType!
}

type Author {
  id: ID!
  name: String!
  posts: [Post!]! @hasInverse(field: "author")
  friends: [Author!]!
}

enum PostType {
  Statement
  Question
  Answer
}